/* parser generated by jison 0.4.13 */
/*
  Returns a Parser object of the following structure:

  Parser: {
    yy: {}
  }

  Parser.prototype: {
    yy: {},
    trace: function(),
    symbols_: {associative list: name ==> number},
    terminals_: {associative list: number ==> name},
    productions_: [...],
    performAction: function anonymous(yytext, yyleng, yylineno, yy, yystate, $$, _$),
    table: [...],
    defaultActions: {...},
    parseError: function(str, hash),
    parse: function(input),

    lexer: {
        EOF: 1,
        parseError: function(str, hash),
        setInput: function(input),
        input: function(),
        unput: function(str),
        more: function(),
        less: function(n),
        pastInput: function(),
        upcomingInput: function(),
        showPosition: function(),
        test_match: function(regex_match_array, rule_index),
        next: function(),
        lex: function(),
        begin: function(condition),
        popState: function(),
        _currentRules: function(),
        topState: function(),
        pushState: function(condition),

        options: {
            ranges: boolean           (optional: true ==> token location info will include a .range[] member)
            flex: boolean             (optional: true ==> flex-like lexing behaviour where the rules are tested exhaustively to find the longest match)
            backtrack_lexer: boolean  (optional: true ==> lexer regexes are tested in order and for each matching regex the action code is invoked; the lexer terminates the scan when a token is returned by the action code)
        },

        performAction: function(yy, yy_, $avoiding_name_collisions, YY_START),
        rules: [...],
        conditions: {associative list: name ==> set},
    }
  }


  token location info (@$, _$, etc.): {
    first_line: n,
    last_line: n,
    first_column: n,
    last_column: n,
    range: [start_number, end_number]       (where the numbers are indexes into the input string, regular zero-based)
  }


  the parseError function receives a 'hash' object with these members for lexer and parser errors: {
    text:        (matched text)
    token:       (the produced terminal token, if any)
    line:        (yylineno)
  }
  while parser (grammar) errors will also provide these members, i.e. parser errors deliver a superset of attributes: {
    loc:         (yylloc)
    expected:    (string describing the set of expected tokens)
    recoverable: (boolean: TRUE when the parser has a error recovery rule available for this particular error)
  }
*/
var pl0 = (function(){
var parser = {trace: function trace() { },
yy: {},
symbols_: {"error":2,"prog":3,"block":4,".":5,"EOF":6,"cyv":7,"procs":8,"statement":9,"consts":10,"vars":11,"CONST":12,"ID":13,"=":14,"NUMBER":15,"r_consts":16,";":17,",":18,"VAR":19,"r_vars":20,"proc":21,"PROCEDURE":22,"args":23,"e":24,"CALL":25,"BEGIN":26,"statement_r":27,"END":28,"IF":29,"condition":30,"THEN":31,"ELSE":32,"WHILE":33,"DO":34,"(":35,"args_r":36,")":37,"ODD":38,"COMP":39,"PI":40,"E":41,"+":42,"-":43,"*":44,"/":45,"^":46,"!":47,"%":48,"$accept":0,"$end":1},
terminals_: {2:"error",5:".",6:"EOF",12:"CONST",13:"ID",14:"=",15:"NUMBER",17:";",18:",",19:"VAR",22:"PROCEDURE",25:"CALL",26:"BEGIN",28:"END",29:"IF",31:"THEN",32:"ELSE",33:"WHILE",34:"DO",35:"(",37:")",38:"ODD",39:"COMP",40:"PI",41:"E",42:"+",43:"-",44:"*",45:"/",46:"^",47:"!",48:"%"},
productions_: [0,[3,3],[4,3],[7,2],[10,0],[10,6],[16,0],[16,5],[11,0],[11,4],[20,0],[20,3],[8,0],[8,2],[21,6],[9,3],[9,3],[9,4],[9,4],[9,6],[9,4],[27,0],[27,3],[23,0],[23,4],[36,0],[36,3],[30,2],[30,3],[24,3],[24,3],[24,3],[24,3],[24,3],[24,3],[24,3],[24,3],[24,2],[24,2],[24,2],[24,3],[24,1],[24,1],[24,1],[24,1]],
performAction: function anonymous(yytext, yyleng, yylineno, yy, yystate /* action[1] */, $$ /* vstack */, _$ /* lstack */) {
/* this == yyval */

var $0 = $$.length - 1;
switch (yystate) {
case 1: 
          this.$ = { type: 'program', block: $$[$0-2], symbol_table: ambitos[0][0], usados: aux_use };
          return this.$;
        
break;
case 2:
          this.$ = { type: 'block', procs: $$[$0-1], st: $$[$0] };
        
break;
case 3:
          ambitos.push([{}]);
        
break;
case 5:
          ambitos[ambitos.length - 1][ambitos[ambitos.length - 1].length - 1][$$[$0-4]] = { type: 'const', value: $$[$0-2] };

          //this.$ = [ { type: 'const', id: $$[$0-4], value: $$[$0-2] } ];
          //if ($$[$0-1]) this.$ = this.$.concat($$[$0-1]);
        
break;
case 7:
          ambitos[ambitos.length - 1][ambitos[ambitos.length - 1].length - 1][$$[$0-3]] = { type: 'const', value: $$[$0-1] };
          
          //this.$ = [ { type: 'const', id: $$[$0-3], value: $$[$0-1] } ];
          //if ($$[$0]) this.$ = this.$.concat($$[$0]);
        
break;
case 9:
          ambitos[ambitos.length - 1][ambitos[ambitos.length - 1].length - 1][$$[$0-2]] = { type: 'var' };
          
          //this.$ = [ { type: 'var', id: $$[$0-2] } ];
          //if ($$[$0-1]) this.$ = this.$.concat($$[$0-1]);
        
break;
case 11:
          ambitos[ambitos.length - 1][ambitos[ambitos.length - 1].length - 1][$$[$0-1]] = { type: 'var' };
          
          //this.$ = [ { type: 'var', id: $$[$0-1] } ];
          //if ($$[$0]) this.$ = this.$.concat($$[$0]);
        
break;
case 12:     
          ambitos.pop(); 
        
break;
case 13:

          this.$ = [$$[$0-1]];
          if ($$[$0]) this.$ = this.$.concat($$[$0]);
          
          if (ambitos[ambitos.length - 1].length == 0)
            ambitos.pop();
        
break;
case 14:

          this.$ = { type: 'procedure', id: $$[$0-4], arguments: $$[$0-3], block: $$[$0-1], symbol_table: ambitos[ambitos.length - 1][0], usados: aux_use };

          ambitos[ambitos.length - 2][ambitos[ambitos.length - 1].length - 1][$$[$0-4]] = { type: 'procedure', arguments: $$[$0-3]? $$[$0-3].length : 0 };
        
          ambitos[ambitos.length - 1] = [{}];
        
break;
case 15: 
          aux_use[$$[$0-2]] = { type: 'var' };
          this.$ = { type: '=', left: { type: 'ID', value: $$[$0-2] }, right: $$[$0] }; 
        
break;
case 16: this.$ = { type: 'CALL', id: $$[$0-1], arguments: $$[$0] }; 
break;
case 17: 
          var v_sts = [$$[$0-2]];
          if ($$[$0-1]) v_sts = v_sts.concat($$[$0-1]);
          this.$ = { type: 'BEGIN', statements: v_sts }; 
        
break;
case 18: this.$ = { type: 'IF', condition: $$[$0-2], statement: $$[$0] }; 
break;
case 19: this.$ = { type: 'IF', condition: $$[$0-4], true_st: $$[$0-2], false_st: $$[$0] }; 
break;
case 20: this.$ = { type: 'WHILE', condition: $$[$0-2], statement: $$[$0] }; 
break;
case 22:
          this.$ = [$$[$0-1]];
          if ($$[$0]) this.$ = this.$.concat($$[$0]);
        
break;
case 23: this.$ = []; 
break;
case 24:
          this.$ = [{ type: 'ARG', value: $$[$0-2] }];
          if ($$[$0-1]) this.$ = this.$.concat($$[$0-1]);
        
break;
case 26:
          this.$ = [{ type: 'ARG', value: $$[$0-1] }]
          if ($$[$0]) this.$ = this.$.concat($$[$0]);
        
break;
case 27: this.$ = { type: 'ODD', e: $$[$0] }; 
break;
case 28: this.$ = { type: $$[$0-1], left: $$[$0-2], right: $$[$0] }; 
break;
case 29:
          aux_use[$$[$0-2]] = { type: 'var' }
          this.$ = { type: '=', left: { type: 'ID', value: $$[$0-2] }, right: $$[$0] }; 
        
break;
case 30: throw new Error("Can't assign to constant 'π'"); 
break;
case 31: throw new Error("Can't assign to math constant 'e'"); 
break;
case 32:this.$ = { type: '+', left: $$[$0-2], right: $$[$0] }; 
break;
case 33:this.$ = { type: '-', left: $$[$0-2], right: $$[$0] }; 
break;
case 34:this.$ = { type: '*', left: $$[$0-2], right: $$[$0] }; 
break;
case 35:this.$ = { type: '/', left: $$[$0-2], right: $$[$0] }; 
break;
case 36:this.$ = { type: '^', left: $$[$0-2], right: $$[$0] }; 
break;
case 37:this.$ = { type: '!', left: $$[$0-1] }; 
break;
case 38:this.$ = { type: '%', left: $$[$0-1] }; 
break;
case 39:this.$ = { type: '-', right: $$[$0] }; 
break;
case 40:this.$ = $$[$0-1];
break;
case 41:this.$ = { type: 'NUM', value: Number(yytext) };
break;
case 42:this.$ = Math.E;
break;
case 43:this.$ = Math.PI;
break;
case 44: 
          aux_use[$$[$0]] = { type: 'var' }
          this.$ = { type: 'ID', value: $$[$0] };
        
break;
}
},
table: [{3:1,4:2,7:3,10:4,12:[1,5],13:[2,4],19:[2,4],22:[2,4],25:[2,4],26:[2,4],29:[2,4],33:[2,4]},{1:[3]},{5:[1,6]},{8:7,13:[2,12],21:8,22:[1,9],25:[2,12],26:[2,12],29:[2,12],33:[2,12]},{11:10,13:[2,8],19:[1,11],22:[2,8],25:[2,8],26:[2,8],29:[2,8],33:[2,8]},{13:[1,12]},{6:[1,13]},{9:14,13:[1,15],25:[1,16],26:[1,17],29:[1,18],33:[1,19]},{8:20,13:[2,12],21:8,22:[1,9],25:[2,12],26:[2,12],29:[2,12],33:[2,12]},{13:[1,21]},{13:[2,3],22:[2,3],25:[2,3],26:[2,3],29:[2,3],33:[2,3]},{13:[1,22]},{14:[1,23]},{1:[2,1]},{5:[2,2],17:[2,2]},{14:[1,24]},{13:[1,25]},{9:26,13:[1,15],25:[1,16],26:[1,17],29:[1,18],33:[1,19]},{13:[1,30],15:[1,35],24:29,30:27,35:[1,34],38:[1,28],40:[1,31],41:[1,32],43:[1,33]},{13:[1,30],15:[1,35],24:29,30:36,35:[1,34],38:[1,28],40:[1,31],41:[1,32],43:[1,33]},{13:[2,13],25:[2,13],26:[2,13],29:[2,13],33:[2,13]},{17:[2,23],23:37,35:[1,38]},{17:[2,10],18:[1,40],20:39},{15:[1,41]},{13:[1,30],15:[1,35],24:42,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{5:[2,23],17:[2,23],23:43,28:[2,23],32:[2,23],35:[1,38]},{17:[1,45],27:44,28:[2,21]},{31:[1,46]},{13:[1,30],15:[1,35],24:47,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{39:[1,48],42:[1,49],43:[1,50],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{5:[2,44],14:[1,56],17:[2,44],18:[2,44],28:[2,44],31:[2,44],32:[2,44],34:[2,44],37:[2,44],39:[2,44],42:[2,44],43:[2,44],44:[2,44],45:[2,44],46:[2,44],47:[2,44],48:[2,44]},{5:[2,43],14:[1,57],17:[2,43],18:[2,43],28:[2,43],31:[2,43],32:[2,43],34:[2,43],37:[2,43],39:[2,43],42:[2,43],43:[2,43],44:[2,43],45:[2,43],46:[2,43],47:[2,43],48:[2,43]},{5:[2,42],14:[1,58],17:[2,42],18:[2,42],28:[2,42],31:[2,42],32:[2,42],34:[2,42],37:[2,42],39:[2,42],42:[2,42],43:[2,42],44:[2,42],45:[2,42],46:[2,42],47:[2,42],48:[2,42]},{13:[1,30],15:[1,35],24:59,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{13:[1,30],15:[1,35],24:60,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{5:[2,41],17:[2,41],18:[2,41],28:[2,41],31:[2,41],32:[2,41],34:[2,41],37:[2,41],39:[2,41],42:[2,41],43:[2,41],44:[2,41],45:[2,41],46:[2,41],47:[2,41],48:[2,41]},{34:[1,61]},{17:[1,62]},{13:[1,30],15:[1,35],24:63,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{17:[1,64]},{13:[1,65]},{16:66,17:[2,6],18:[1,67]},{5:[2,15],17:[2,15],28:[2,15],32:[2,15],42:[1,49],43:[1,50],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{5:[2,16],17:[2,16],28:[2,16],32:[2,16]},{28:[1,68]},{9:69,13:[1,15],25:[1,16],26:[1,17],29:[1,18],33:[1,19]},{9:70,13:[1,15],25:[1,16],26:[1,17],29:[1,18],33:[1,19]},{31:[2,27],34:[2,27],42:[1,49],43:[1,50],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{13:[1,30],15:[1,35],24:71,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{13:[1,30],15:[1,35],24:72,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{13:[1,30],15:[1,35],24:73,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{13:[1,30],15:[1,35],24:74,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{13:[1,30],15:[1,35],24:75,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{13:[1,30],15:[1,35],24:76,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{5:[2,37],17:[2,37],18:[2,37],28:[2,37],31:[2,37],32:[2,37],34:[2,37],37:[2,37],39:[2,37],42:[2,37],43:[2,37],44:[2,37],45:[2,37],46:[2,37],47:[2,37],48:[2,37]},{5:[2,38],17:[2,38],18:[2,38],28:[2,38],31:[2,38],32:[2,38],34:[2,38],37:[2,38],39:[2,38],42:[2,38],43:[2,38],44:[2,38],45:[2,38],46:[2,38],47:[2,38],48:[2,38]},{13:[1,30],15:[1,35],24:77,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{13:[1,30],15:[1,35],24:78,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{13:[1,30],15:[1,35],24:79,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{5:[2,39],17:[2,39],18:[2,39],28:[2,39],31:[2,39],32:[2,39],34:[2,39],37:[2,39],39:[2,39],42:[2,39],43:[2,39],44:[2,39],45:[2,39],46:[2,39],47:[1,54],48:[2,39]},{37:[1,80],42:[1,49],43:[1,50],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{9:81,13:[1,15],25:[1,16],26:[1,17],29:[1,18],33:[1,19]},{4:82,7:3,10:4,12:[1,5],13:[2,4],19:[2,4],22:[2,4],25:[2,4],26:[2,4],29:[2,4],33:[2,4]},{18:[1,84],36:83,37:[2,25],42:[1,49],43:[1,50],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{13:[2,9],22:[2,9],25:[2,9],26:[2,9],29:[2,9],33:[2,9]},{17:[2,10],18:[1,40],20:85},{17:[1,86]},{13:[1,87]},{5:[2,17],17:[2,17],28:[2,17],32:[2,17]},{17:[1,45],27:88,28:[2,21]},{5:[2,18],17:[2,18],28:[2,18],32:[1,89]},{31:[2,28],34:[2,28],42:[1,49],43:[1,50],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{5:[2,32],17:[2,32],18:[2,32],28:[2,32],31:[2,32],32:[2,32],34:[2,32],37:[2,32],39:[2,32],42:[2,32],43:[2,32],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{5:[2,33],17:[2,33],18:[2,33],28:[2,33],31:[2,33],32:[2,33],34:[2,33],37:[2,33],39:[2,33],42:[2,33],43:[2,33],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{5:[2,34],17:[2,34],18:[2,34],28:[2,34],31:[2,34],32:[2,34],34:[2,34],37:[2,34],39:[2,34],42:[2,34],43:[2,34],44:[2,34],45:[2,34],46:[1,53],47:[1,54],48:[1,55]},{5:[2,35],17:[2,35],18:[2,35],28:[2,35],31:[2,35],32:[2,35],34:[2,35],37:[2,35],39:[2,35],42:[2,35],43:[2,35],44:[2,35],45:[2,35],46:[1,53],47:[1,54],48:[1,55]},{5:[2,36],17:[2,36],18:[2,36],28:[2,36],31:[2,36],32:[2,36],34:[2,36],37:[2,36],39:[2,36],42:[2,36],43:[2,36],44:[2,36],45:[2,36],46:[2,36],47:[1,54],48:[1,55]},{5:[2,29],17:[2,29],18:[2,29],28:[2,29],31:[2,29],32:[2,29],34:[2,29],37:[2,29],39:[2,29],42:[1,49],43:[1,50],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{5:[2,30],17:[2,30],18:[2,30],28:[2,30],31:[2,30],32:[2,30],34:[2,30],37:[2,30],39:[2,30],42:[1,49],43:[1,50],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{5:[2,31],17:[2,31],18:[2,31],28:[2,31],31:[2,31],32:[2,31],34:[2,31],37:[2,31],39:[2,31],42:[1,49],43:[1,50],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{5:[2,40],17:[2,40],18:[2,40],28:[2,40],31:[2,40],32:[2,40],34:[2,40],37:[2,40],39:[2,40],42:[2,40],43:[2,40],44:[2,40],45:[2,40],46:[2,40],47:[2,40],48:[2,40]},{5:[2,20],17:[2,20],28:[2,20],32:[2,20]},{17:[1,90]},{37:[1,91]},{13:[1,30],15:[1,35],24:92,35:[1,34],40:[1,31],41:[1,32],43:[1,33]},{17:[2,11]},{13:[2,5],19:[2,5],22:[2,5],25:[2,5],26:[2,5],29:[2,5],33:[2,5]},{14:[1,93]},{28:[2,22]},{9:94,13:[1,15],25:[1,16],26:[1,17],29:[1,18],33:[1,19]},{13:[2,14],22:[2,14],25:[2,14],26:[2,14],29:[2,14],33:[2,14]},{5:[2,24],17:[2,24],28:[2,24],32:[2,24]},{18:[1,84],36:95,37:[2,25],42:[1,49],43:[1,50],44:[1,51],45:[1,52],46:[1,53],47:[1,54],48:[1,55]},{15:[1,96]},{5:[2,19],17:[2,19],28:[2,19],32:[2,19]},{37:[2,26]},{16:97,17:[2,6],18:[1,67]},{17:[2,7]}],
defaultActions: {13:[2,1],85:[2,11],88:[2,22],95:[2,26],97:[2,7]},
parseError: function parseError(str, hash) {
    if (hash.recoverable) {
        this.trace(str);
    } else {
        throw new Error(str);
    }
},
parse: function parse(input) {
    var self = this, stack = [0], vstack = [null], lstack = [], table = this.table, yytext = '', yylineno = 0, yyleng = 0, recovering = 0, TERROR = 2, EOF = 1;
    var args = lstack.slice.call(arguments, 1);
    this.lexer.setInput(input);
    this.lexer.yy = this.yy;
    this.yy.lexer = this.lexer;
    this.yy.parser = this;
    if (typeof this.lexer.yylloc == 'undefined') {
        this.lexer.yylloc = {};
    }
    var yyloc = this.lexer.yylloc;
    lstack.push(yyloc);
    var ranges = this.lexer.options && this.lexer.options.ranges;
    if (typeof this.yy.parseError === 'function') {
        this.parseError = this.yy.parseError;
    } else {
        this.parseError = Object.getPrototypeOf(this).parseError;
    }
    function popStack(n) {
        stack.length = stack.length - 2 * n;
        vstack.length = vstack.length - n;
        lstack.length = lstack.length - n;
    }
    function lex() {
        var token;
        token = self.lexer.lex() || EOF;
        if (typeof token !== 'number') {
            token = self.symbols_[token] || token;
        }
        return token;
    }
    var symbol, preErrorSymbol, state, action, a, r, yyval = {}, p, len, newState, expected;
    while (true) {
        state = stack[stack.length - 1];
        if (this.defaultActions[state]) {
            action = this.defaultActions[state];
        } else {
            if (symbol === null || typeof symbol == 'undefined') {
                symbol = lex();
            }
            action = table[state] && table[state][symbol];
        }
                    if (typeof action === 'undefined' || !action.length || !action[0]) {
                var errStr = '';
                expected = [];
                for (p in table[state]) {
                    if (this.terminals_[p] && p > TERROR) {
                        expected.push('\'' + this.terminals_[p] + '\'');
                    }
                }
                if (this.lexer.showPosition) {
                    errStr = 'Parse error on line ' + (yylineno + 1) + ':\n' + this.lexer.showPosition() + '\nExpecting ' + expected.join(', ') + ', got \'' + (this.terminals_[symbol] || symbol) + '\'';
                } else {
                    errStr = 'Parse error on line ' + (yylineno + 1) + ': Unexpected ' + (symbol == EOF ? 'end of input' : '\'' + (this.terminals_[symbol] || symbol) + '\'');
                }
                this.parseError(errStr, {
                    text: this.lexer.match,
                    token: this.terminals_[symbol] || symbol,
                    line: this.lexer.yylineno,
                    loc: yyloc,
                    expected: expected
                });
            }
        if (action[0] instanceof Array && action.length > 1) {
            throw new Error('Parse Error: multiple actions possible at state: ' + state + ', token: ' + symbol);
        }
        switch (action[0]) {
        case 1:
            stack.push(symbol);
            vstack.push(this.lexer.yytext);
            lstack.push(this.lexer.yylloc);
            stack.push(action[1]);
            symbol = null;
            if (!preErrorSymbol) {
                yyleng = this.lexer.yyleng;
                yytext = this.lexer.yytext;
                yylineno = this.lexer.yylineno;
                yyloc = this.lexer.yylloc;
                if (recovering > 0) {
                    recovering--;
                }
            } else {
                symbol = preErrorSymbol;
                preErrorSymbol = null;
            }
            break;
        case 2:
            len = this.productions_[action[1]][1];
            yyval.$ = vstack[vstack.length - len];
            yyval._$ = {
                first_line: lstack[lstack.length - (len || 1)].first_line,
                last_line: lstack[lstack.length - 1].last_line,
                first_column: lstack[lstack.length - (len || 1)].first_column,
                last_column: lstack[lstack.length - 1].last_column
            };
            if (ranges) {
                yyval._$.range = [
                    lstack[lstack.length - (len || 1)].range[0],
                    lstack[lstack.length - 1].range[1]
                ];
            }
            r = this.performAction.apply(yyval, [
                yytext,
                yyleng,
                yylineno,
                this.yy,
                action[1],
                vstack,
                lstack
            ].concat(args));
            if (typeof r !== 'undefined') {
                return r;
            }
            if (len) {
                stack = stack.slice(0, -1 * len * 2);
                vstack = vstack.slice(0, -1 * len);
                lstack = lstack.slice(0, -1 * len);
            }
            stack.push(this.productions_[action[1]][0]);
            vstack.push(yyval.$);
            lstack.push(yyval._$);
            newState = table[stack[stack.length - 2]][stack[stack.length - 1]];
            stack.push(newState);
            break;
        case 3:
            return true;
        }
    }
    return true;
}};

  var aux_use = {};
  
  var ambitos = [[{}]];
  
  function fact (n) { 
    return n==0 ? 1 : fact(n-1) * n 
}

/* generated by jison-lex 0.2.1 */
var lexer = (function(){
var lexer = {

EOF:1,

parseError:function parseError(str, hash) {
        if (this.yy.parser) {
            this.yy.parser.parseError(str, hash);
        } else {
            throw new Error(str);
        }
    },

// resets the lexer, sets new input
setInput:function (input) {
        this._input = input;
        this._more = this._backtrack = this.done = false;
        this.yylineno = this.yyleng = 0;
        this.yytext = this.matched = this.match = '';
        this.conditionStack = ['INITIAL'];
        this.yylloc = {
            first_line: 1,
            first_column: 0,
            last_line: 1,
            last_column: 0
        };
        if (this.options.ranges) {
            this.yylloc.range = [0,0];
        }
        this.offset = 0;
        return this;
    },

// consumes and returns one char from the input
input:function () {
        var ch = this._input[0];
        this.yytext += ch;
        this.yyleng++;
        this.offset++;
        this.match += ch;
        this.matched += ch;
        var lines = ch.match(/(?:\r\n?|\n).*/g);
        if (lines) {
            this.yylineno++;
            this.yylloc.last_line++;
        } else {
            this.yylloc.last_column++;
        }
        if (this.options.ranges) {
            this.yylloc.range[1]++;
        }

        this._input = this._input.slice(1);
        return ch;
    },

// unshifts one char (or a string) into the input
unput:function (ch) {
        var len = ch.length;
        var lines = ch.split(/(?:\r\n?|\n)/g);

        this._input = ch + this._input;
        this.yytext = this.yytext.substr(0, this.yytext.length - len - 1);
        //this.yyleng -= len;
        this.offset -= len;
        var oldLines = this.match.split(/(?:\r\n?|\n)/g);
        this.match = this.match.substr(0, this.match.length - 1);
        this.matched = this.matched.substr(0, this.matched.length - 1);

        if (lines.length - 1) {
            this.yylineno -= lines.length - 1;
        }
        var r = this.yylloc.range;

        this.yylloc = {
            first_line: this.yylloc.first_line,
            last_line: this.yylineno + 1,
            first_column: this.yylloc.first_column,
            last_column: lines ?
                (lines.length === oldLines.length ? this.yylloc.first_column : 0)
                 + oldLines[oldLines.length - lines.length].length - lines[0].length :
              this.yylloc.first_column - len
        };

        if (this.options.ranges) {
            this.yylloc.range = [r[0], r[0] + this.yyleng - len];
        }
        this.yyleng = this.yytext.length;
        return this;
    },

// When called from action, caches matched text and appends it on next action
more:function () {
        this._more = true;
        return this;
    },

// When called from action, signals the lexer that this rule fails to match the input, so the next matching rule (regex) should be tested instead.
reject:function () {
        if (this.options.backtrack_lexer) {
            this._backtrack = true;
        } else {
            return this.parseError('Lexical error on line ' + (this.yylineno + 1) + '. You can only invoke reject() in the lexer when the lexer is of the backtracking persuasion (options.backtrack_lexer = true).\n' + this.showPosition(), {
                text: "",
                token: null,
                line: this.yylineno
            });

        }
        return this;
    },

// retain first n characters of the match
less:function (n) {
        this.unput(this.match.slice(n));
    },

// displays already matched input, i.e. for error messages
pastInput:function () {
        var past = this.matched.substr(0, this.matched.length - this.match.length);
        return (past.length > 20 ? '...':'') + past.substr(-20).replace(/\n/g, "");
    },

// displays upcoming input, i.e. for error messages
upcomingInput:function () {
        var next = this.match;
        if (next.length < 20) {
            next += this._input.substr(0, 20-next.length);
        }
        return (next.substr(0,20) + (next.length > 20 ? '...' : '')).replace(/\n/g, "");
    },

// displays the character position where the lexing error occurred, i.e. for error messages
showPosition:function () {
        var pre = this.pastInput();
        var c = new Array(pre.length + 1).join("-");
        return pre + this.upcomingInput() + "\n" + c + "^";
    },

// test the lexed token: return FALSE when not a match, otherwise return token
test_match:function (match, indexed_rule) {
        var token,
            lines,
            backup;

        if (this.options.backtrack_lexer) {
            // save context
            backup = {
                yylineno: this.yylineno,
                yylloc: {
                    first_line: this.yylloc.first_line,
                    last_line: this.last_line,
                    first_column: this.yylloc.first_column,
                    last_column: this.yylloc.last_column
                },
                yytext: this.yytext,
                match: this.match,
                matches: this.matches,
                matched: this.matched,
                yyleng: this.yyleng,
                offset: this.offset,
                _more: this._more,
                _input: this._input,
                yy: this.yy,
                conditionStack: this.conditionStack.slice(0),
                done: this.done
            };
            if (this.options.ranges) {
                backup.yylloc.range = this.yylloc.range.slice(0);
            }
        }

        lines = match[0].match(/(?:\r\n?|\n).*/g);
        if (lines) {
            this.yylineno += lines.length;
        }
        this.yylloc = {
            first_line: this.yylloc.last_line,
            last_line: this.yylineno + 1,
            first_column: this.yylloc.last_column,
            last_column: lines ?
                         lines[lines.length - 1].length - lines[lines.length - 1].match(/\r?\n?/)[0].length :
                         this.yylloc.last_column + match[0].length
        };
        this.yytext += match[0];
        this.match += match[0];
        this.matches = match;
        this.yyleng = this.yytext.length;
        if (this.options.ranges) {
            this.yylloc.range = [this.offset, this.offset += this.yyleng];
        }
        this._more = false;
        this._backtrack = false;
        this._input = this._input.slice(match[0].length);
        this.matched += match[0];
        token = this.performAction.call(this, this.yy, this, indexed_rule, this.conditionStack[this.conditionStack.length - 1]);
        if (this.done && this._input) {
            this.done = false;
        }
        if (token) {
            return token;
        } else if (this._backtrack) {
            // recover context
            for (var k in backup) {
                this[k] = backup[k];
            }
            return false; // rule action called reject() implying the next rule should be tested instead.
        }
        return false;
    },

// return next match in input
next:function () {
        if (this.done) {
            return this.EOF;
        }
        if (!this._input) {
            this.done = true;
        }

        var token,
            match,
            tempMatch,
            index;
        if (!this._more) {
            this.yytext = '';
            this.match = '';
        }
        var rules = this._currentRules();
        for (var i = 0; i < rules.length; i++) {
            tempMatch = this._input.match(this.rules[rules[i]]);
            if (tempMatch && (!match || tempMatch[0].length > match[0].length)) {
                match = tempMatch;
                index = i;
                if (this.options.backtrack_lexer) {
                    token = this.test_match(tempMatch, rules[i]);
                    if (token !== false) {
                        return token;
                    } else if (this._backtrack) {
                        match = false;
                        continue; // rule action called reject() implying a rule MISmatch.
                    } else {
                        // else: this is a lexer rule which consumes input without producing a token (e.g. whitespace)
                        return false;
                    }
                } else if (!this.options.flex) {
                    break;
                }
            }
        }
        if (match) {
            token = this.test_match(match, rules[index]);
            if (token !== false) {
                return token;
            }
            // else: this is a lexer rule which consumes input without producing a token (e.g. whitespace)
            return false;
        }
        if (this._input === "") {
            return this.EOF;
        } else {
            return this.parseError('Lexical error on line ' + (this.yylineno + 1) + '. Unrecognized text.\n' + this.showPosition(), {
                text: "",
                token: null,
                line: this.yylineno
            });
        }
    },

// return next match that has a token
lex:function lex() {
        var r = this.next();
        if (r) {
            return r;
        } else {
            return this.lex();
        }
    },

// activates a new lexer condition state (pushes the new lexer condition state onto the condition stack)
begin:function begin(condition) {
        this.conditionStack.push(condition);
    },

// pop the previously active lexer condition state off the condition stack
popState:function popState() {
        var n = this.conditionStack.length - 1;
        if (n > 0) {
            return this.conditionStack.pop();
        } else {
            return this.conditionStack[0];
        }
    },

// produce the lexer rule set which is active for the currently active lexer condition state
_currentRules:function _currentRules() {
        if (this.conditionStack.length && this.conditionStack[this.conditionStack.length - 1]) {
            return this.conditions[this.conditionStack[this.conditionStack.length - 1]].rules;
        } else {
            return this.conditions["INITIAL"].rules;
        }
    },

// return the currently active lexer condition state; when an index argument is provided it produces the N-th previous condition state, if available
topState:function topState(n) {
        n = this.conditionStack.length - 1 - Math.abs(n || 0);
        if (n >= 0) {
            return this.conditionStack[n];
        } else {
            return "INITIAL";
        }
    },

// alias for begin(condition)
pushState:function pushState(condition) {
        this.begin(condition);
    },

// return the number of states currently on the stack
stateStackSize:function stateStackSize() {
        return this.conditionStack.length;
    },
options: {},
performAction: function anonymous(yy,yy_,$avoiding_name_collisions,YY_START) {

var reserved_words = { PI: 'PI', VAR: 'VAR', CONST: 'CONST', E: 'E', PROCEDURE: 'PROCEDURE', BEGIN: 'BEGIN', END: 'END', ODD: 'ODD', CALL: 'CALL', IF: 'IF', THEN: 'THEN', ELSE: 'ELSE', WHILE: 'WHILE', DO: 'DO' }

function idORrw(x) {
  return (x.toUpperCase() in reserved_words)? x.toUpperCase() : 'ID'
}


var YYSTATE=YY_START;
switch($avoiding_name_collisions) {
case 0:/* skip whitespace and comments */
break;
case 1:return 15
break;
case 2:return idORrw(yy_.yytext)
break;
case 3:return 39
break;
case 4:return yy_.yytext
break;
case 5:return 6
break;
case 6:return 'INVALID'
break;
}
},
rules: [/^(?:\s+|#.*)/,/^(?:\b\d+(\.\d*)?([eE][-+]?\d+)?\b)/,/^(?:\b[A-Za-z_]\w*\b)/,/^(?:[<>=!][=]|[<>#])/,/^(?:[-*/+()!=;,.^%])/,/^(?:$)/,/^(?:.)/],
conditions: {"INITIAL":{"rules":[0,1,2,3,4,5,6],"inclusive":true}}
};
return lexer;
})();
parser.lexer = lexer;
function Parser () {
  this.yy = {};
}
Parser.prototype = parser;parser.Parser = Parser;
return new Parser;
})();


if (typeof require !== 'undefined' && typeof exports !== 'undefined') {
exports.parser = pl0;
exports.Parser = pl0.Parser;
exports.parse = function () { return pl0.parse.apply(pl0, arguments); };
exports.main = function commonjsMain(args) {
    if (!args[1]) {
        console.log('Usage: '+args[0]+' FILE');
        process.exit(1);
    }
    var source = require('fs').readFileSync(require('path').normalize(args[1]), "utf8");
    return exports.parser.parse(source);
};
if (typeof module !== 'undefined' && require.main === module) {
  exports.main(process.argv.slice(1));
}
}