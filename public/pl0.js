/* parser generated by jison 0.4.13 */
/*
  Returns a Parser object of the following structure:

  Parser: {
    yy: {}
  }

  Parser.prototype: {
    yy: {},
    trace: function(),
    symbols_: {associative list: name ==> number},
    terminals_: {associative list: number ==> name},
    productions_: [...],
    performAction: function anonymous(yytext, yyleng, yylineno, yy, yystate, $$, _$),
    table: [...],
    defaultActions: {...},
    parseError: function(str, hash),
    parse: function(input),

    lexer: {
        EOF: 1,
        parseError: function(str, hash),
        setInput: function(input),
        input: function(),
        unput: function(str),
        more: function(),
        less: function(n),
        pastInput: function(),
        upcomingInput: function(),
        showPosition: function(),
        test_match: function(regex_match_array, rule_index),
        next: function(),
        lex: function(),
        begin: function(condition),
        popState: function(),
        _currentRules: function(),
        topState: function(),
        pushState: function(condition),

        options: {
            ranges: boolean           (optional: true ==> token location info will include a .range[] member)
            flex: boolean             (optional: true ==> flex-like lexing behaviour where the rules are tested exhaustively to find the longest match)
            backtrack_lexer: boolean  (optional: true ==> lexer regexes are tested in order and for each matching regex the action code is invoked; the lexer terminates the scan when a token is returned by the action code)
        },

        performAction: function(yy, yy_, $avoiding_name_collisions, YY_START),
        rules: [...],
        conditions: {associative list: name ==> set},
    }
  }


  token location info (@$, _$, etc.): {
    first_line: n,
    last_line: n,
    first_column: n,
    last_column: n,
    range: [start_number, end_number]       (where the numbers are indexes into the input string, regular zero-based)
  }


  the parseError function receives a 'hash' object with these members for lexer and parser errors: {
    text:        (matched text)
    token:       (the produced terminal token, if any)
    line:        (yylineno)
  }
  while parser (grammar) errors will also provide these members, i.e. parser errors deliver a superset of attributes: {
    loc:         (yylloc)
    expected:    (string describing the set of expected tokens)
    recoverable: (boolean: TRUE when the parser has a error recovery rule available for this particular error)
  }
*/
var pl0 = (function(){
var parser = {trace: function trace() { },
yy: {},
symbols_: {"error":2,"prog":3,"statement":4,".":5,"EOF":6,"expressions":7,"s":8,";":9,"e":10,"ID":11,"=":12,"CALL":13,"args":14,"BEGIN":15,"statement_r":16,"END":17,"IF":18,"condition":19,"THEN":20,"ELSE":21,"WHILE":22,"DO":23,"(":24,"args_r":25,")":26,",":27,"ODD":28,"==":29,"#":30,"<":31,"<=":32,">":33,">=":34,"PI":35,"E":36,"+":37,"-":38,"*":39,"/":40,"^":41,"!":42,"%":43,"NUMBER":44,"$accept":0,"$end":1},
terminals_: {2:"error",5:".",6:"EOF",9:";",11:"ID",12:"=",13:"CALL",15:"BEGIN",17:"END",18:"IF",20:"THEN",21:"ELSE",22:"WHILE",23:"DO",24:"(",26:")",27:",",28:"ODD",29:"==",30:"#",31:"<",32:"<=",33:">",34:">=",35:"PI",36:"E",37:"+",38:"-",39:"*",40:"/",41:"^",42:"!",43:"%",44:"NUMBER"},
productions_: [0,[3,3],[7,1],[7,3],[8,0],[8,1],[4,3],[4,3],[4,4],[4,4],[4,6],[4,4],[16,0],[16,3],[14,0],[14,4],[25,0],[25,3],[19,2],[19,3],[19,3],[19,3],[19,3],[19,3],[19,3],[10,3],[10,3],[10,3],[10,3],[10,3],[10,3],[10,3],[10,3],[10,2],[10,2],[10,2],[10,3],[10,1],[10,1],[10,1],[10,1]],
performAction: function anonymous(yytext, yyleng, yylineno, yy, yystate /* action[1] */, $$ /* vstack */, _$ /* lstack */) {
/* this == yyval */

var $0 = $$.length - 1;
switch (yystate) {
case 1: 
          this.$ = { type: 'program', block: $$[$0-2] };
          return this.$;
        
break;
case 2: this.$ = (typeof $$[$0] === 'undefined')? [] : [ $$[$0] ]; 
break;
case 3: this.$ = $$[$0-2];
          if ($$[$0]) this.$.push($$[$0]); 
          console.log(this.$);
        
break;
case 6: this.$ = { type: '=', left: $$[$0-2], right: $$[$0] }; 
break;
case 7: this.$ = { type: 'CALL', id: $$[$0-1], arguments: $$[$0] }; 
break;
case 8: 
          var v_sts = [$$[$0-2]];
          if ($$[$0-1]) v_sts = v_sts.concat($$[$0-1]);
          this.$ = { type: 'BEGIN', statements: v_sts }; 
        
break;
case 9: this.$ = { type: 'IF', condition: $$[$0-2], statement: $$[$0] }; 
break;
case 10: this.$ = { type: 'IF', condition: $$[$0-4], true_st: $$[$0-2], false_st: $$[$0] }; 
break;
case 11: this.$ = { type: 'WHILE', condition: $$[$0-2], statement: $$[$0] }; 
break;
case 13:
          this.$ = [$$[$0-1]];
          if ($$[$0]) this.$ = this.$.concat($$[$0]);
        
break;
case 15:
          this.$ = [$$[$0-2]];
          if ($$[$0-1]) this.$ = this.$.concat($$[$0-1]);
        
break;
case 17:
          this.$ = [$$[$0-1]]
          if ($$[$0]) this.$ = this.$.concat($$[$0]);
        
break;
case 18: this.$ = { type: 'ODD', e: $$[$0] }; 
break;
case 19: this.$ = { type: $$[$0-1], left: $$[$0-2], right: $$[$0] }; 
break;
case 20: this.$ = { type: $$[$0-1], left: $$[$0-2], right: $$[$0] }; 
break;
case 21: this.$ = { type: $$[$0-1], left: $$[$0-2], right: $$[$0] }; 
break;
case 22: this.$ = { type: $$[$0-1], left: $$[$0-2], right: $$[$0] }; 
break;
case 23: this.$ = { type: $$[$0-1], left: $$[$0-2], right: $$[$0] }; 
break;
case 24: this.$ = { type: $$[$0-1], left: $$[$0-2], right: $$[$0] }; 
break;
case 25: symbol_table[$$[$0-2]] = this.$ = $$[$0]; 
break;
case 26: throw new Error("Can't assign to constant 'π'"); 
break;
case 27: throw new Error("Can't assign to math constant 'e'"); 
break;
case 28:this.$ = $$[$0-2]+$$[$0];
break;
case 29:this.$ = $$[$0-2]-$$[$0];
break;
case 30:this.$ = $$[$0-2]*$$[$0];
break;
case 31:
          if ($$[$0] == 0) throw new Error("Division by zero, error!");
          this.$ = $$[$0-2]/$$[$0];
        
break;
case 32:this.$ = Math.pow($$[$0-2], $$[$0]);
break;
case 33:
          if ($$[$0-1] % 1 !== 0) 
             throw "Error! Attempt to compute the factorial of "+
                   "a floating point number "+$$[$0-1];
          this.$ = fact($$[$0-1]);
        
break;
case 34:this.$ = $$[$0-1]/100;
break;
case 35:this.$ = -$$[$0];
break;
case 36:this.$ = $$[$0-1];
break;
case 37:this.$ = Number(yytext);
break;
case 38:this.$ = Math.E;
break;
case 39:this.$ = Math.PI;
break;
case 40: this.$ = symbol_table[yytext] || 0; 
break;
}
},
table: [{3:1,4:2,11:[1,3],13:[1,4],15:[1,5],18:[1,6],22:[1,7]},{1:[3]},{5:[1,8]},{12:[1,9]},{11:[1,10]},{4:11,11:[1,3],13:[1,4],15:[1,5],18:[1,6],22:[1,7]},{10:14,11:[1,15],19:12,24:[1,19],28:[1,13],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:14,11:[1,15],19:21,24:[1,19],28:[1,13],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{6:[1,22]},{10:23,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{5:[2,14],9:[2,14],14:24,17:[2,14],21:[2,14],24:[1,25]},{9:[1,27],16:26,17:[2,12]},{20:[1,28]},{10:29,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{29:[1,30],30:[1,31],31:[1,32],32:[1,33],33:[1,34],34:[1,35],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{5:[2,40],9:[2,40],12:[1,43],17:[2,40],20:[2,40],21:[2,40],23:[2,40],26:[2,40],29:[2,40],30:[2,40],31:[2,40],32:[2,40],33:[2,40],34:[2,40],37:[2,40],38:[2,40],39:[2,40],40:[2,40],41:[2,40],42:[2,40],43:[2,40]},{5:[2,39],9:[2,39],12:[1,44],17:[2,39],20:[2,39],21:[2,39],23:[2,39],26:[2,39],29:[2,39],30:[2,39],31:[2,39],32:[2,39],33:[2,39],34:[2,39],37:[2,39],38:[2,39],39:[2,39],40:[2,39],41:[2,39],42:[2,39],43:[2,39]},{5:[2,38],9:[2,38],12:[1,45],17:[2,38],20:[2,38],21:[2,38],23:[2,38],26:[2,38],29:[2,38],30:[2,38],31:[2,38],32:[2,38],33:[2,38],34:[2,38],37:[2,38],38:[2,38],39:[2,38],40:[2,38],41:[2,38],42:[2,38],43:[2,38]},{10:46,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:47,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{5:[2,37],9:[2,37],17:[2,37],20:[2,37],21:[2,37],23:[2,37],26:[2,37],29:[2,37],30:[2,37],31:[2,37],32:[2,37],33:[2,37],34:[2,37],37:[2,37],38:[2,37],39:[2,37],40:[2,37],41:[2,37],42:[2,37],43:[2,37]},{23:[1,48]},{1:[2,1]},{5:[2,6],9:[2,6],17:[2,6],21:[2,6],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{5:[2,7],9:[2,7],17:[2,7],21:[2,7]},{11:[1,49]},{17:[1,50]},{4:51,11:[1,3],13:[1,4],15:[1,5],18:[1,6],22:[1,7]},{4:52,11:[1,3],13:[1,4],15:[1,5],18:[1,6],22:[1,7]},{20:[2,18],23:[2,18],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{10:53,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:54,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:55,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:56,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:57,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:58,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:59,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:60,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:61,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:62,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:63,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{5:[2,33],9:[2,33],17:[2,33],20:[2,33],21:[2,33],23:[2,33],26:[2,33],29:[2,33],30:[2,33],31:[2,33],32:[2,33],33:[2,33],34:[2,33],37:[2,33],38:[2,33],39:[2,33],40:[2,33],41:[2,33],42:[2,33],43:[2,33]},{5:[2,34],9:[2,34],17:[2,34],20:[2,34],21:[2,34],23:[2,34],26:[2,34],29:[2,34],30:[2,34],31:[2,34],32:[2,34],33:[2,34],34:[2,34],37:[2,34],38:[2,34],39:[2,34],40:[2,34],41:[2,34],42:[2,34],43:[2,34]},{10:64,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:65,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{10:66,11:[1,15],24:[1,19],35:[1,16],36:[1,17],38:[1,18],44:[1,20]},{5:[2,35],9:[2,35],17:[2,35],20:[2,35],21:[2,35],23:[2,35],26:[2,35],29:[2,35],30:[2,35],31:[2,35],32:[2,35],33:[2,35],34:[2,35],37:[2,35],38:[2,35],39:[2,35],40:[2,35],41:[2,35],42:[1,41],43:[2,35]},{26:[1,67],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{4:68,11:[1,3],13:[1,4],15:[1,5],18:[1,6],22:[1,7]},{25:69,26:[2,16],27:[1,70]},{5:[2,8],9:[2,8],17:[2,8],21:[2,8]},{9:[1,27],16:71,17:[2,12]},{5:[2,9],9:[2,9],17:[2,9],21:[1,72]},{20:[2,19],23:[2,19],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{20:[2,20],23:[2,20],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{20:[2,21],23:[2,21],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{20:[2,22],23:[2,22],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{20:[2,23],23:[2,23],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{20:[2,24],23:[2,24],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{5:[2,28],9:[2,28],17:[2,28],20:[2,28],21:[2,28],23:[2,28],26:[2,28],29:[2,28],30:[2,28],31:[2,28],32:[2,28],33:[2,28],34:[2,28],37:[2,28],38:[2,28],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{5:[2,29],9:[2,29],17:[2,29],20:[2,29],21:[2,29],23:[2,29],26:[2,29],29:[2,29],30:[2,29],31:[2,29],32:[2,29],33:[2,29],34:[2,29],37:[2,29],38:[2,29],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{5:[2,30],9:[2,30],17:[2,30],20:[2,30],21:[2,30],23:[2,30],26:[2,30],29:[2,30],30:[2,30],31:[2,30],32:[2,30],33:[2,30],34:[2,30],37:[2,30],38:[2,30],39:[2,30],40:[2,30],41:[1,40],42:[1,41],43:[1,42]},{5:[2,31],9:[2,31],17:[2,31],20:[2,31],21:[2,31],23:[2,31],26:[2,31],29:[2,31],30:[2,31],31:[2,31],32:[2,31],33:[2,31],34:[2,31],37:[2,31],38:[2,31],39:[2,31],40:[2,31],41:[1,40],42:[1,41],43:[1,42]},{5:[2,32],9:[2,32],17:[2,32],20:[2,32],21:[2,32],23:[2,32],26:[2,32],29:[2,32],30:[2,32],31:[2,32],32:[2,32],33:[2,32],34:[2,32],37:[2,32],38:[2,32],39:[2,32],40:[2,32],41:[2,32],42:[1,41],43:[1,42]},{5:[2,25],9:[2,25],17:[2,25],20:[2,25],21:[2,25],23:[2,25],26:[2,25],29:[2,25],30:[2,25],31:[2,25],32:[2,25],33:[2,25],34:[2,25],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{5:[2,26],9:[2,26],17:[2,26],20:[2,26],21:[2,26],23:[2,26],26:[2,26],29:[2,26],30:[2,26],31:[2,26],32:[2,26],33:[2,26],34:[2,26],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{5:[2,27],9:[2,27],17:[2,27],20:[2,27],21:[2,27],23:[2,27],26:[2,27],29:[2,27],30:[2,27],31:[2,27],32:[2,27],33:[2,27],34:[2,27],37:[1,36],38:[1,37],39:[1,38],40:[1,39],41:[1,40],42:[1,41],43:[1,42]},{5:[2,36],9:[2,36],17:[2,36],20:[2,36],21:[2,36],23:[2,36],26:[2,36],29:[2,36],30:[2,36],31:[2,36],32:[2,36],33:[2,36],34:[2,36],37:[2,36],38:[2,36],39:[2,36],40:[2,36],41:[2,36],42:[2,36],43:[2,36]},{5:[2,11],9:[2,11],17:[2,11],21:[2,11]},{26:[1,73]},{11:[1,74]},{17:[2,13]},{4:75,11:[1,3],13:[1,4],15:[1,5],18:[1,6],22:[1,7]},{5:[2,15],9:[2,15],17:[2,15],21:[2,15]},{25:76,26:[2,16],27:[1,70]},{5:[2,10],9:[2,10],17:[2,10],21:[2,10]},{26:[2,17]}],
defaultActions: {22:[2,1],71:[2,13],76:[2,17]},
parseError: function parseError(str, hash) {
    if (hash.recoverable) {
        this.trace(str);
    } else {
        throw new Error(str);
    }
},
parse: function parse(input) {
    var self = this, stack = [0], vstack = [null], lstack = [], table = this.table, yytext = '', yylineno = 0, yyleng = 0, recovering = 0, TERROR = 2, EOF = 1;
    var args = lstack.slice.call(arguments, 1);
    this.lexer.setInput(input);
    this.lexer.yy = this.yy;
    this.yy.lexer = this.lexer;
    this.yy.parser = this;
    if (typeof this.lexer.yylloc == 'undefined') {
        this.lexer.yylloc = {};
    }
    var yyloc = this.lexer.yylloc;
    lstack.push(yyloc);
    var ranges = this.lexer.options && this.lexer.options.ranges;
    if (typeof this.yy.parseError === 'function') {
        this.parseError = this.yy.parseError;
    } else {
        this.parseError = Object.getPrototypeOf(this).parseError;
    }
    function popStack(n) {
        stack.length = stack.length - 2 * n;
        vstack.length = vstack.length - n;
        lstack.length = lstack.length - n;
    }
    function lex() {
        var token;
        token = self.lexer.lex() || EOF;
        if (typeof token !== 'number') {
            token = self.symbols_[token] || token;
        }
        return token;
    }
    var symbol, preErrorSymbol, state, action, a, r, yyval = {}, p, len, newState, expected;
    while (true) {
        state = stack[stack.length - 1];
        if (this.defaultActions[state]) {
            action = this.defaultActions[state];
        } else {
            if (symbol === null || typeof symbol == 'undefined') {
                symbol = lex();
            }
            action = table[state] && table[state][symbol];
        }
                    if (typeof action === 'undefined' || !action.length || !action[0]) {
                var errStr = '';
                expected = [];
                for (p in table[state]) {
                    if (this.terminals_[p] && p > TERROR) {
                        expected.push('\'' + this.terminals_[p] + '\'');
                    }
                }
                if (this.lexer.showPosition) {
                    errStr = 'Parse error on line ' + (yylineno + 1) + ':\n' + this.lexer.showPosition() + '\nExpecting ' + expected.join(', ') + ', got \'' + (this.terminals_[symbol] || symbol) + '\'';
                } else {
                    errStr = 'Parse error on line ' + (yylineno + 1) + ': Unexpected ' + (symbol == EOF ? 'end of input' : '\'' + (this.terminals_[symbol] || symbol) + '\'');
                }
                this.parseError(errStr, {
                    text: this.lexer.match,
                    token: this.terminals_[symbol] || symbol,
                    line: this.lexer.yylineno,
                    loc: yyloc,
                    expected: expected
                });
            }
        if (action[0] instanceof Array && action.length > 1) {
            throw new Error('Parse Error: multiple actions possible at state: ' + state + ', token: ' + symbol);
        }
        switch (action[0]) {
        case 1:
            stack.push(symbol);
            vstack.push(this.lexer.yytext);
            lstack.push(this.lexer.yylloc);
            stack.push(action[1]);
            symbol = null;
            if (!preErrorSymbol) {
                yyleng = this.lexer.yyleng;
                yytext = this.lexer.yytext;
                yylineno = this.lexer.yylineno;
                yyloc = this.lexer.yylloc;
                if (recovering > 0) {
                    recovering--;
                }
            } else {
                symbol = preErrorSymbol;
                preErrorSymbol = null;
            }
            break;
        case 2:
            len = this.productions_[action[1]][1];
            yyval.$ = vstack[vstack.length - len];
            yyval._$ = {
                first_line: lstack[lstack.length - (len || 1)].first_line,
                last_line: lstack[lstack.length - 1].last_line,
                first_column: lstack[lstack.length - (len || 1)].first_column,
                last_column: lstack[lstack.length - 1].last_column
            };
            if (ranges) {
                yyval._$.range = [
                    lstack[lstack.length - (len || 1)].range[0],
                    lstack[lstack.length - 1].range[1]
                ];
            }
            r = this.performAction.apply(yyval, [
                yytext,
                yyleng,
                yylineno,
                this.yy,
                action[1],
                vstack,
                lstack
            ].concat(args));
            if (typeof r !== 'undefined') {
                return r;
            }
            if (len) {
                stack = stack.slice(0, -1 * len * 2);
                vstack = vstack.slice(0, -1 * len);
                lstack = lstack.slice(0, -1 * len);
            }
            stack.push(this.productions_[action[1]][0]);
            vstack.push(yyval.$);
            lstack.push(yyval._$);
            newState = table[stack[stack.length - 2]][stack[stack.length - 1]];
            stack.push(newState);
            break;
        case 3:
            return true;
        }
    }
    return true;
}};

var symbol_table = {};

function fact (n) { 
  return n==0 ? 1 : fact(n-1) * n 
}

/* generated by jison-lex 0.2.1 */
var lexer = (function(){
var lexer = {

EOF:1,

parseError:function parseError(str, hash) {
        if (this.yy.parser) {
            this.yy.parser.parseError(str, hash);
        } else {
            throw new Error(str);
        }
    },

// resets the lexer, sets new input
setInput:function (input) {
        this._input = input;
        this._more = this._backtrack = this.done = false;
        this.yylineno = this.yyleng = 0;
        this.yytext = this.matched = this.match = '';
        this.conditionStack = ['INITIAL'];
        this.yylloc = {
            first_line: 1,
            first_column: 0,
            last_line: 1,
            last_column: 0
        };
        if (this.options.ranges) {
            this.yylloc.range = [0,0];
        }
        this.offset = 0;
        return this;
    },

// consumes and returns one char from the input
input:function () {
        var ch = this._input[0];
        this.yytext += ch;
        this.yyleng++;
        this.offset++;
        this.match += ch;
        this.matched += ch;
        var lines = ch.match(/(?:\r\n?|\n).*/g);
        if (lines) {
            this.yylineno++;
            this.yylloc.last_line++;
        } else {
            this.yylloc.last_column++;
        }
        if (this.options.ranges) {
            this.yylloc.range[1]++;
        }

        this._input = this._input.slice(1);
        return ch;
    },

// unshifts one char (or a string) into the input
unput:function (ch) {
        var len = ch.length;
        var lines = ch.split(/(?:\r\n?|\n)/g);

        this._input = ch + this._input;
        this.yytext = this.yytext.substr(0, this.yytext.length - len - 1);
        //this.yyleng -= len;
        this.offset -= len;
        var oldLines = this.match.split(/(?:\r\n?|\n)/g);
        this.match = this.match.substr(0, this.match.length - 1);
        this.matched = this.matched.substr(0, this.matched.length - 1);

        if (lines.length - 1) {
            this.yylineno -= lines.length - 1;
        }
        var r = this.yylloc.range;

        this.yylloc = {
            first_line: this.yylloc.first_line,
            last_line: this.yylineno + 1,
            first_column: this.yylloc.first_column,
            last_column: lines ?
                (lines.length === oldLines.length ? this.yylloc.first_column : 0)
                 + oldLines[oldLines.length - lines.length].length - lines[0].length :
              this.yylloc.first_column - len
        };

        if (this.options.ranges) {
            this.yylloc.range = [r[0], r[0] + this.yyleng - len];
        }
        this.yyleng = this.yytext.length;
        return this;
    },

// When called from action, caches matched text and appends it on next action
more:function () {
        this._more = true;
        return this;
    },

// When called from action, signals the lexer that this rule fails to match the input, so the next matching rule (regex) should be tested instead.
reject:function () {
        if (this.options.backtrack_lexer) {
            this._backtrack = true;
        } else {
            return this.parseError('Lexical error on line ' + (this.yylineno + 1) + '. You can only invoke reject() in the lexer when the lexer is of the backtracking persuasion (options.backtrack_lexer = true).\n' + this.showPosition(), {
                text: "",
                token: null,
                line: this.yylineno
            });

        }
        return this;
    },

// retain first n characters of the match
less:function (n) {
        this.unput(this.match.slice(n));
    },

// displays already matched input, i.e. for error messages
pastInput:function () {
        var past = this.matched.substr(0, this.matched.length - this.match.length);
        return (past.length > 20 ? '...':'') + past.substr(-20).replace(/\n/g, "");
    },

// displays upcoming input, i.e. for error messages
upcomingInput:function () {
        var next = this.match;
        if (next.length < 20) {
            next += this._input.substr(0, 20-next.length);
        }
        return (next.substr(0,20) + (next.length > 20 ? '...' : '')).replace(/\n/g, "");
    },

// displays the character position where the lexing error occurred, i.e. for error messages
showPosition:function () {
        var pre = this.pastInput();
        var c = new Array(pre.length + 1).join("-");
        return pre + this.upcomingInput() + "\n" + c + "^";
    },

// test the lexed token: return FALSE when not a match, otherwise return token
test_match:function (match, indexed_rule) {
        var token,
            lines,
            backup;

        if (this.options.backtrack_lexer) {
            // save context
            backup = {
                yylineno: this.yylineno,
                yylloc: {
                    first_line: this.yylloc.first_line,
                    last_line: this.last_line,
                    first_column: this.yylloc.first_column,
                    last_column: this.yylloc.last_column
                },
                yytext: this.yytext,
                match: this.match,
                matches: this.matches,
                matched: this.matched,
                yyleng: this.yyleng,
                offset: this.offset,
                _more: this._more,
                _input: this._input,
                yy: this.yy,
                conditionStack: this.conditionStack.slice(0),
                done: this.done
            };
            if (this.options.ranges) {
                backup.yylloc.range = this.yylloc.range.slice(0);
            }
        }

        lines = match[0].match(/(?:\r\n?|\n).*/g);
        if (lines) {
            this.yylineno += lines.length;
        }
        this.yylloc = {
            first_line: this.yylloc.last_line,
            last_line: this.yylineno + 1,
            first_column: this.yylloc.last_column,
            last_column: lines ?
                         lines[lines.length - 1].length - lines[lines.length - 1].match(/\r?\n?/)[0].length :
                         this.yylloc.last_column + match[0].length
        };
        this.yytext += match[0];
        this.match += match[0];
        this.matches = match;
        this.yyleng = this.yytext.length;
        if (this.options.ranges) {
            this.yylloc.range = [this.offset, this.offset += this.yyleng];
        }
        this._more = false;
        this._backtrack = false;
        this._input = this._input.slice(match[0].length);
        this.matched += match[0];
        token = this.performAction.call(this, this.yy, this, indexed_rule, this.conditionStack[this.conditionStack.length - 1]);
        if (this.done && this._input) {
            this.done = false;
        }
        if (token) {
            return token;
        } else if (this._backtrack) {
            // recover context
            for (var k in backup) {
                this[k] = backup[k];
            }
            return false; // rule action called reject() implying the next rule should be tested instead.
        }
        return false;
    },

// return next match in input
next:function () {
        if (this.done) {
            return this.EOF;
        }
        if (!this._input) {
            this.done = true;
        }

        var token,
            match,
            tempMatch,
            index;
        if (!this._more) {
            this.yytext = '';
            this.match = '';
        }
        var rules = this._currentRules();
        for (var i = 0; i < rules.length; i++) {
            tempMatch = this._input.match(this.rules[rules[i]]);
            if (tempMatch && (!match || tempMatch[0].length > match[0].length)) {
                match = tempMatch;
                index = i;
                if (this.options.backtrack_lexer) {
                    token = this.test_match(tempMatch, rules[i]);
                    if (token !== false) {
                        return token;
                    } else if (this._backtrack) {
                        match = false;
                        continue; // rule action called reject() implying a rule MISmatch.
                    } else {
                        // else: this is a lexer rule which consumes input without producing a token (e.g. whitespace)
                        return false;
                    }
                } else if (!this.options.flex) {
                    break;
                }
            }
        }
        if (match) {
            token = this.test_match(match, rules[index]);
            if (token !== false) {
                return token;
            }
            // else: this is a lexer rule which consumes input without producing a token (e.g. whitespace)
            return false;
        }
        if (this._input === "") {
            return this.EOF;
        } else {
            return this.parseError('Lexical error on line ' + (this.yylineno + 1) + '. Unrecognized text.\n' + this.showPosition(), {
                text: "",
                token: null,
                line: this.yylineno
            });
        }
    },

// return next match that has a token
lex:function lex() {
        var r = this.next();
        if (r) {
            return r;
        } else {
            return this.lex();
        }
    },

// activates a new lexer condition state (pushes the new lexer condition state onto the condition stack)
begin:function begin(condition) {
        this.conditionStack.push(condition);
    },

// pop the previously active lexer condition state off the condition stack
popState:function popState() {
        var n = this.conditionStack.length - 1;
        if (n > 0) {
            return this.conditionStack.pop();
        } else {
            return this.conditionStack[0];
        }
    },

// produce the lexer rule set which is active for the currently active lexer condition state
_currentRules:function _currentRules() {
        if (this.conditionStack.length && this.conditionStack[this.conditionStack.length - 1]) {
            return this.conditions[this.conditionStack[this.conditionStack.length - 1]].rules;
        } else {
            return this.conditions["INITIAL"].rules;
        }
    },

// return the currently active lexer condition state; when an index argument is provided it produces the N-th previous condition state, if available
topState:function topState(n) {
        n = this.conditionStack.length - 1 - Math.abs(n || 0);
        if (n >= 0) {
            return this.conditionStack[n];
        } else {
            return "INITIAL";
        }
    },

// alias for begin(condition)
pushState:function pushState(condition) {
        this.begin(condition);
    },

// return the number of states currently on the stack
stateStackSize:function stateStackSize() {
        return this.conditionStack.length;
    },
options: {},
performAction: function anonymous(yy,yy_,$avoiding_name_collisions,YY_START) {

var reserved_words = { PI: 'PI', E: 'E', BEGIN: 'BEGIN', END: 'END', ODD: 'ODD', CALL: 'CALL', IF: 'IF', THEN: 'THEN', ELSE: 'ELSE', WHILE: 'WHILE', DO: 'DO' }

function idORrw(x) {
  return (x.toUpperCase() in reserved_words)? x.toUpperCase() : 'ID'
}


var YYSTATE=YY_START;
switch($avoiding_name_collisions) {
case 0:/* skip whitespace and comments */
break;
case 1:return 44
break;
case 2:return idORrw(yy_.yytext)
break;
case 3:return yy_.yytext;
break;
case 4:return 6
break;
case 5:return 'INVALID'
break;
}
},
rules: [/^(?:\s+|#.*)/,/^(?:\b\d+(\.\d*)?([eE][-+]?\d+)?\b)/,/^(?:\b[A-Za-z_]\w*\b)/,/^(?:[<>=!][=]|[-*/+();,.<>=^!%])/,/^(?:$)/,/^(?:.)/],
conditions: {"INITIAL":{"rules":[0,1,2,3,4,5],"inclusive":true}}
};
return lexer;
})();
parser.lexer = lexer;
function Parser () {
  this.yy = {};
}
Parser.prototype = parser;parser.Parser = Parser;
return new Parser;
})();


if (typeof require !== 'undefined' && typeof exports !== 'undefined') {
exports.parser = pl0;
exports.Parser = pl0.Parser;
exports.parse = function () { return pl0.parse.apply(pl0, arguments); };
exports.main = function commonjsMain(args) {
    if (!args[1]) {
        console.log('Usage: '+args[0]+' FILE');
        process.exit(1);
    }
    var source = require('fs').readFileSync(require('path').normalize(args[1]), "utf8");
    return exports.parser.parse(source);
};
if (typeof module !== 'undefined' && require.main === module) {
  exports.main(process.argv.slice(1));
}
}